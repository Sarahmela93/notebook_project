This is a react app project.
Inside you will find a note app creation.


To begin with , after cloning the project , open it with ```npm start```.

If you don't have the react-uuid library , install it with ```npm install react-uuid```.
Same for the mardown library ```npm install react-markdown```
